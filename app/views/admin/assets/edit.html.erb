<!-- Small Nav -->
<div class="small-nav">
        <a href="#">Dashboard</a>
        <span>&gt;</span>
        tour
</div>
<!-- End Small Nav -->

<!-- Message Error -->
<% if @tour.errors.any? %>
  <div class="msg msg-error">
     <% @tour.errors.full_messages.each do |msg| %>
       <p><strong><%= msg %></strong></p>
     <% end %>
  </div>
<% end %>
<!-- End Message Error -->
<br />
<!-- Main -->
<div id="main">
        <div class="cl">&nbsp;</div>

        <!-- Content -->
        <div id="content">
          <!-- Box -->
          <div class="box">
            <!-- Box Head -->
            <div class="box-head">
              <h2 class="left">Edit tour</h2>
            </div>
            <!-- End Box Head -->
            <%= form_for([:admin, @category, @tour]) do |f| %>
              <%= render :partial => "form", :object => f %>
              <div class="buttons">
                  <%= f.submit "Save" %>
                  <%= link_to "Cancel", admin_category_tours_path(@category) %>
              </div>
            <% end %>
          </div>
          <!-- End Box -->
        </div>
        <!-- End Content -->
        <!-- Sidebar -->
        <div id="sidebar">
          <!-- Box -->
          <div class="box">
            <!-- Box Head -->
            <div class="box-head">
              <h2>Management</h2>
            </div>
            <!-- End Box Head-->
            <div class="box-content">
              <%= link_to(content_tag(:span, "Add new tour"), new_admin_category_tour_path(@category), :class => 'add-button') %>
              <%= link_to(content_tag(:span, "Delete tour"), admin_category_tour_path(@category, @tour), :confirm => 'Are you sure?', :method => :delete, :title => 'Delete tour') %>
              <div class="cl">&nbsp;</div>
            </div>
          </div>
          <!-- End Box -->
        </div>
        <!-- End Sidebar -->

        <div class="cl">&nbsp;</div>			
</div>
<!-- Main -->
	